
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/succursaledb
    username: postgres
    password: '#LddndAemadn1879#'

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  jpa:
    show-sql: true
    generate-ddl: true
    open-in-view: false
    hibernate.ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


#springdoc:
#  swagger-ui:
#    path: /swagger-ui/api-docs.html
